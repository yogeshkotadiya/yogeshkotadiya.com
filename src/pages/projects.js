import React from 'react';
import Header from '../components/Header';

const projects = () => {
  return (
    <React.Fragment>
      <Header />
      <div>
        <h1>Projects</h1>
      </div>
    </React.Fragment>
  );
};

export default projects;
